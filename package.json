{
  "name": "android-private-mcp",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node bridge/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check bridge/client.js && node --check bridge/server.js && npm test"
  },
  "dependencies": { "@modelcontextprotocol/sdk": "1.30.0", "zod": "4.5.4" }
}
